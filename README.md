To preview the website locally, install Ruby and [bundler](https://bundler.io/). Then run

    $ bundle install
    $ bundle exec jekyll serve

Then follow the instructions printed on screen to view the page in your browser.
